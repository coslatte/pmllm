# Core dependencies for pmllm
# This file is generated from pyproject.toml for compatibility
# Prefer using: pip install -e . or uv pip install -e .

neo4j>=6.0.3
pymilvus>=2.6.3
requests>=2.31.0
sentence-transformers>=2.2.2

# Additional recommended dependencies for development
# torch>=2.0.0  # Required by sentence-transformers, install separately if needed
# transformers>=4.30.0  # May be needed for some models
